# SDR2HDR_pro のプロジェクトルール

このファイルは、このプロジェクトの会話でユーザーが示した目的、指示、訂正を、今後の作業に適用するためのルールである。
アシスタントの未承認の提案や、その後に撤回された説明を要件として扱わない。
上位の指示と併せて読み、現在のユーザーの明示的な指示・訂正を優先する。

## アプリの目的

- このアプリの目的は、SDR映像をHDRへ変換し、指定された出力形式・色空間で保存することである。
- ユーザーが好みの色へ仕上げるための色調整アプリとして、目的を説明し直したり機能を広げたりしない。
- 共通のSDR→HDR処理には既存のAI処理やプリセットがある。それらの存在と、ProRes・HEVC・OpenEXR・ACEScgへの出力処理の正しさは、別の論点として扱う。
- 「色調整アプリではない」という説明を、既存のAI処理・プリセットを削除する指示と解釈しない。
- 他製品の紹介文は比較用の資料であり、紹介文中の全機能をこのアプリの要件にしない。クラウドAPI、Runway連携、編集ソフト連携などを依頼なしに追加しない。

## 作業対象を混同しない

作業を始める際に、依頼が次のどの部分を対象としているか、会話と現行コードで確認する。

1. 共通のSDR→HDR処理：既存のAIモデル、明るさの処理、プリセットなど。
2. 出力用の色変換：PQ、HLG、ACEScgなどの色空間・伝達特性・明るさの単位の変換。
3. 保存処理：ProRes、HEVC、OpenEXRの圧縮、ビット深度、連番、色情報の記録。

- 出力用の変換や保存について質問された際に、共通のAI補正や彩度設定を別の問題として持ち出さない。
- 保存形式、色空間、HDRの伝達特性を同じものとして説明しない。
- 現在の実装、今回の変更、未実装の提案、実験だけの処理を区別する。
- 対象外の既存処理に問題らしい点を見つけても、その存在だけを理由に作業範囲を広げない。

## 標準仕様に従う変換

- 出力用の変換は、対象の規格・版に対応する公式仕様、参照実装、公式ライブラリの定義を基準にする。
- 公式ライブラリを呼ぶだけで適合と判断しない。入力と出力の色空間、変換方向、明るさの単位、基準ピーク、対象バージョンを実際の処理経路と照合する。
- ProResのビット深度・色情報、PQ/HLGの伝達特性、ACESの版を、それぞれ対応する仕様に基づいて確認する。
- 「往復すると数値が違う」だけで不具合と判断しない。標準が意図する変化、圧縮・量子化による差、実装の誤りを切り分ける。
- 数値を合わせるために、公式変換の切り詰め、色域処理、カーブなどを勝手に削除・置換しない。
- 編集ソフトで出力に合った入力カラー設定を使う通常の扱いと、独自の表示変換を要求する方式を区別する。独自の表示変換を出力対応の解決策として持ち込まない。
- ACESの往復差を消すために専用ビューを使う案は、この会話では撤回済みである。本体へ導入する承認はない。
- 差の大小を示す場合は、単位、対象画像、比較段階、測定方法を示す。数値差から見た目の悪さや実害を断定しない。

## 調査と実装の進め方

- 原因調査・不具合修正・新規実装では `shusei-flow` を読み、依頼された段階に対応する手順を使う。
- Graphifyが利用できる場合はコード探索・影響範囲の確認に使い、その結果を現行ソースで照合する。古いグラフだけで結論を出さない。
- 進行用スキルだけで技術判断を済ませず、対象技術の公式資料や必要な専門スキルも確認する。
- `plan-and-run-goal` を毎回自動的に適用しない。単に複数の工程があることを理由に、計画承認やGoal登録を追加しない。
- 「対応できるか」の調査では、APIや選択肢の存在確認だけで終えず、対象環境で判断に必要な実行・出力の根拠まで確認する。判断できない場合は、具体的な不足と理由を示す。
- 調査だけの依頼では本体やテストを編集しない。実装を明示されたローカルで可逆的な範囲は、同じ承認を求め直さず進める。
- 実装対象、維持条件、対象外、検証方法を短く示し、承認範囲に必要な最小の変更を行う。
- このプロジェクトでは、依頼された実装を先に揃え、その後に関連テストの編集・実行へ進む。テスト整備を先行させて本体実装を後回しにしない。事前の仕様確認や再現に必要な実測は別である。
- 実装後の検証で誤りが見つかった場合は、原因を確認して承認範囲内で修正し、必要な再検証を行う。
- 同梱モデル、学習データ、既存の出力経路、音声、フレーム数・順序、キャンセル時の既存データなど、依頼外の対象を勝手に変更しない。
- ユーザーの未コミット変更や、この作業前から存在する変更を上書き・復元・削除しない。

## テストの目的と合格条件

- テストを通すことを目的にしない。ユーザーの依頼と標準仕様に沿って、実際の出力が正しいかを確認するために使う。
- 既存テストも正しいとは限らない。出力0件でも成功する検査、設定値の確認だけで実出力を保証したことにする検査などを見直す。
- 期待値、許容誤差、比較対象は、要求・仕様・保存精度に根拠を置く。結果に合わせて許容誤差を広げたり、比較対象を差し替えたりしない。
- 逆に、根拠なく「完全一致しなければ不合格」という条件を追加しない。標準処理の往復差を、アプリの不具合と決めつけない。
- 比較条件自体が誤っていた場合は、理由を確認したうえで修正する。修正後のテストが何を証明し、何を証明しないかを明記する。
- 公式変換の往復結果との一致は、公式処理との整合性の検査である。変換前との完全一致や、画質上の問題の解消を示す検査と混同しない。
- 保存処理を変更した場合は、必要に応じて実ファイルを読み戻し、色情報、実際の精度、画素、音声、フレーム数、連番を確認する。12bitというラベルだけで12bit相当の階調を証明したことにしない。
- 変更した処理とその周辺を優先して検証する。無関係なテストや実装を増やさない。全体テストは共通基盤など変更の影響が広い場合に、その理由を示して行う。
- 検証が成功した後は、新しい変更・失敗・未解決の懸念がない限り、同じ検証を繰り返さない。
- ビルド、テスト、実出力、GUI操作、実際のHDR表示での見え方は別々の確認である。必要な受入条件だけを定め、実施していない確認を成功と報告しない。
- 編集ソフトへの読み込みや主観的な画質評価などを、依頼されていない追加の完了条件にしない。

## 会話の読み方と説明

- 直近の文だけでなく、関連する依頼、承認、除外、訂正を統合して答える。以前の条件は、変更された部分だけ更新する。
- 引用付きの指摘は、引用された文章そのものとユーザーのコメントに答える。別の質問への指摘だと決めつけない。
- ユーザーの訂正を、無関係な機能の削除や、新しい目的の追加へ拡大解釈しない。
- 指摘に同意するためだけに、根拠のない説明へ言い換えない。誤った点を具体的に訂正し、確認済みの事実は維持する。
- 日本語で、非エンジニアにも伝わる言葉を使う。自然なお嬢様口調を保ちつつ、正確さと分かりやすさを優先する。
- 省略しすぎない。専門用語を使う場合は、何を扱う処理か、利用者の操作に何が変わるかを説明する。
- 未確認の効果や実害を事実にしない。問題と断定する前に、ユーザーの要求または標準仕様との不一致を確認する。

## 報告

- 調査は「確認結果」の下に「対象」「確認できたこと」「確認できなかったこと」「次のアクション」を置く。
- 変更は「変更内容」の下に「何を」「どこからどこへ」「その結果どうなったか」「次のアクション」を置く。
- 検証条件、実測結果、確認済みの制約を「確認できなかったこと」へ入れない。
- 実装済み・実測済み・未実装・未確認を分ける。テスト成功だけで全作業を完了扱いにしない。
- 未解決事項を説明書へ書いただけで解決扱いにしない。一方、根拠のない追加条件を未解決事項として作らない。
- 次の作業がなければ「なし（完了）」、承認や確認が不要なら「不要」と記載する。
- コミットは依頼範囲に従い、メッセージは日本語にする。
- 実装・設計・構成を変更した場合は `FOR[hazimeno_ipoo].md` も更新する。説明は現行の事実に基づかせる。

## このルールの根拠

2026年9月7日〜8日のプロジェクト会話「現行アプリの対応状況を調査」で確認したユーザーの指示・訂正を整理した。
特に次の発言を基準にし、アシスタントの撤回済みの提案を採用しない。

- 「拡大解釈せずに指示された事だけに集中」
- 「実装してください。テストはテストを通す事を目的にしないでください。既存のテストも見直してください」
- 「すべての実装を先に済ませろ」
- 「単純にsdrをhdrに変えるアプリだ。色調整するアプリではない」
- 「何でも合わせないとダメって決めつけてんじゃないの？」
- 「これは、別のはなしで、プロれずとかＡＥＳの変換とは違うだろって言ってんだよ」

このファイルは将来の作業規則であり、現行実装の規格適合性や未確認機能の動作を認定するものではない。
