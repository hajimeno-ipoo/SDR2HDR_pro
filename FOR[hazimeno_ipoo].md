# 初心者のためのはじめの一歩ガイド：SDR2HDR

## 1. 目的と現在の状態

このアプリは、通常のSDR動画をHDR10向けに変換するPythonアプリです。画面操作用のGUIと、コマンド操作用のCLIがあります。

使用・改善する学習済みモデルは、最初のモデル `models/enhancement_model_20260310.pt` の1つだけです。このモデルの画質をRubyモデル相当へ近づけることが目的です。Ruby相当の画質の達成は未確認です。複数の候補から良いものを選ぶことを開発目的にしません。

v2、保管版、追加候補のモデルと、不要な比較動画・画像・評価記録は削除しました。LIVEとSol Levanteのデータセット、内容の分割表、学習履歴は残しています。同名のONNXファイルと付随データは、最初のモデルの別形式であり、別の改善候補ではありません。

GUIは最初のモデルだけを「使用モデル」として表示します。別名のモデルや学習途中のファイルは一覧に含めません。最初のモデルがない場合も、別のモデルへ自動で切り替えません。既存の変換計算と汎用の学習機能は維持しています。

## 2. 最初のモデルと変換処理の役割

最初のモデルは3種類の補正用データを出力し、`ai.py`が明るさの拡張、コントラスト、保護の情報として受け取ります。モデルだけで完成したHDR動画を出す構造ではありません。規則による補正とモデルの出力を組み合わせ、`core.py`で色変換、保護、明るさ調整を行います。

このため、モデルの学習中の成績と、最終動画の画質は別に確認します。どの処理が画質上の問題を生んでいるかを特定してから、残したモデルの改善へ結びつけます。学習量の不足を原因と決めつけたり、追加学習だけで改善すると保証したりしません。

コードには5種類の補正情報を使うHDR Guidanceの処理も残っていますが、削除済みのv2モデル向けの経路です。最初の3出力モデルが、v2の目標輝度や復元判定を出すものとして説明しません。

白飛びで失われた元の色や模様の忠実な復元は未確認です。明るさの調整や保護処理が存在することを、復元の成功や無劣化の保証とは扱いません。

## 3. コードの構成

| ファイル | 役割 |
|---|---|
| `src/sdr2hdr/model.py` | モデルの構造。最初のモデル用とHDR Guidance用の定義が残る |
| `src/sdr2hdr/ai.py` | モデルの読込と、出力を補正情報として受け取る処理 |
| `src/sdr2hdr/core.py` | 色変換、保護、補正の適用を行う中心処理 |
| `src/sdr2hdr/hdr_guidance.py` | HDR Guidance用の目標輝度や適用範囲の計算 |
| `src/sdr2hdr/app.py` | 変換ジョブと設定の管理 |
| `src/sdr2hdr/cli.py` / `gui.py` | コマンドと画面の操作窓口 |
| `src/sdr2hdr/io.py` | FFmpegによる読込、書出し、HDR情報の記録 |
| `src/sdr2hdr/review.py` | 変換結果の数値測定。人の見え方の合否を保証するものではない |
| `src/sdr2hdr/pairing.py` | CSVに基づくHDRとSDRの対応づけ |
| `scripts/prepare_data.py` | 学習用データの作成 |
| `scripts/train.py` | 学習処理と学習・検証の分割 |
| `scripts/export_model.py` | 学習済みモデルの書出し |

## 4. 保持するデータと学習時の注意

LIVEの分割表は `data/live_pilot/split.json` です。31内容を学習21、調整5、最終確認5に分けています。最終確認側も過去の評価に使っているため、新しい未使用データとは呼びません。

CSV付きのデータセットは、記載された`video_name`を使ってHDRとSDRを対応づけます。`content_name`、解像度、ビットレートが一致する組を使い、欠落や重複があれば停止します。対応づけのために元動画を推測で改名しません。

学習用の`.npz`には内容名も保存します。同じ場面の別解像度や別圧縮版が学習側と検証側に分かれると、未知の映像への性能を確認したことにならないためです。元動画が1本しかない場合のフレーム分割も、作品をまたぐ評価とは区別します。

共通部分を固定する既存の学習機能では、重みだけでなくBatchNormの内部統計も固定します。勾配を止めるだけでは統計が更新されるため、固定部分を評価モードで扱います。ただし、この機能をそのまま最初のモデルの改善方法として採用したわけではありません。

3モデルの採用比較、v2固定の時間平滑比較、追加候補専用の学習スクリプトは撤去しました。根拠未検証の許容差0による採用判定も撤去しています。データ作成と汎用の学習・測定機能は残し、最初のモデルの改善方法は確認済みの画質上の課題に基づいて決めます。

## 5. 色と出力で間違えやすい点

入力や学習のBT.709と、出力のBT.2020では明るさの計算係数が異なります。異なる基準のRGBをそのまま比較しません。新しい実ペアデータには元のBT.2020 HDRも保持します。

Torchのぼかしと輪郭計算では画像外を黒として扱わず、一様な画像に偽の縁を作らないようにしています。

MaxCLLは処理中の線形RGBの最大値、MaxFALLは各画素のRGB最大値をフレームごとに平均し、その最大を記録します。人の目に合わせた加重平均の輝度と取り違えないよう、色付きの入力でも確認します。黒帯の除外や圧縮後の全画素再測定は行っていないため、HDR情報の記録だけで規格全体への適合を保証しません。

AP1線形EXRは、明るさ調整後のデータをACEScgの原色・白色点へ変換した表示基準の出力です。撮影時の光量を復元したものではありません。FFmpegへ渡す並びはGBRの面順で、出力は元の画像サイズへ戻します。

AP1版は書出し後に`exrstdattr`で原色、白色点、1.0に対応する明るさを記録します。画素を変えず、今回出力した連番だけを処理します。コマンドがなければ開始前に停止します。`colorInteropID`は`unknown`で、読み込み側の自動認識は保証しません。

モデルの`reference_nits`は教師データの明るさの基準で、動画の出力上限とは別です。記録のない最初のモデルは従来の解釈を維持します。記録がないことを理由に値を勝手に追加しません。

## 6. 使い方

CLIでは残したモデルを指定します。

```bash
python -m sdr2hdr.cli input.mp4 output_hdr.mp4 \
  --model-path models/enhancement_model_20260310.pt
```

GUIは次で起動します。

```bash
python -m sdr2hdr.gui
```

Codex用に「GUIを起動」のアクション設定ファイル `.codex/environments/environment.toml` を用意しています。プロジェクト直下の既存の `.venv` を使い、`./.venv/bin/python -m sdr2hdr.gui` を実行します。このコマンドでのGUI起動は確認済みですが、Codex画面での登録・表示は未確認です。設定画面から追加する場合は、アクションの名前に「GUIを起動」、アクションスクリプトにこのコマンドを入力します。依存関係の自動インストールは行わないため、新しい作業フォルダーでは先にPython環境の準備が必要です。

モデル欄の「使用モデル」が使用対象です。動画と画像は同じモデルを使います。詳しい操作と出力形式はREADMEを参照してください。

## GitHubへ置くファイル

公開先は `hajimeno-ipoo/SDR2HDR_pro` です。コード、テスト、依存関係定義、README、この説明書、LICENSE、実行に必要な `models/enhancement_model_20260310.pt` を追跡します。データセット、学習生成物、比較出力、ONNX書出し、旧設計メモ、キャッシュ、秘密設定は追跡しません。これらのローカルファイルは削除しません。ローカルとGitHubの両方で履歴を新規に開始し、以前のGit履歴と送信先設定は引き継ぎません。

## 7. 改善を確認するときの原則

確認するのは、残したモデルへの変更が画質上の問題を改善したか、Rubyとの差を縮めたかです。別の候補を増やして選ぶことへ目的を置き換えません。

数値上の誤差、実際の動画、HDR画面での見え方は別々の根拠です。任意の「許容差0」をユーザー承認済みの品質基準として扱いません。Ruby相当の画質、失われた模様の復元、全作品での品質は、確認できた範囲を超えて断定しません。

今回の整理では、GUIのモデル一覧と不要な実験用入口を変更しました。モデルの再学習、変換処理の変更、追加の比較計算は行っていません。


## 8. ProRes、ACEScg、HLGの出力処理

`output_color.py` は、既存のHDR補正が作った画素を、保存形式に必要な表現へ変換します。AIモデルや学習処理は変更していません。

- HLGは、BT.2020の光の量をXYZへ変換し、OpenColorIOの基準1000 nitのHLG変換を使います。RGBそれぞれに単独のカーブを掛ける方式は、色付きの画素でITUの基準式と一致しないため使いません。HDR10用のPQ情報はHLGに記録しません。
- ACEScgは、OpenColorIO 2.5.2内蔵のACES 1.3／2.0設定を使用します。明るさ調整後のPQから公式出力変換を逆にたどり、ACEScgの16bit浮動小数点EXRへ保存します。版と変換名をEXRに残します。撮影時の光量を復元するものではありません。
- ACESの検証では、保存したACEScg値を公式逆変換の結果と直接比較します。表示用の順変換には切り詰めがあるため、表示結果だけの比較では保存値の違いを見落とす場合があります。標準の往復差を消す目的で公式変換を書き換えたり、完全一致を独自の合格条件にしたりしません。
- 以前の `openexr_acescg` は表示基準AP1として維持します。新しいACES出力は `openexr_acescg_1_3` と `openexr_acescg_2_0` です。既存の呼び出しの色の意味を勝手に変えません。
- ProResでは、FFmpegに色情報を指定してもMOV内に「不明」が残ることを実出力で確認しています。映像内部はFFmpegの `prores_metadata`、MOV内は規定の入れ子構造をたどって色情報を記録します。圧縮済み画素の中から文字列を探して書き換える方法は使いません。一時ファイルで記録を終えた後に置き換え、失敗時には元の出力を保持します。
- 12bitの確認にはファイルの表示だけでなく、10bitの1024段階を超える輝度値を保存・再読込できるかを使います。圧縮があるため、入力の全数値がそのまま残るという意味ではありません。

HLGとACEScgの新形式は動画AI経路に追加しています。Log素材用と画像用は独立した既存経路であり、新形式を選択肢へ混ぜません。

OpenEXRの動画出力は、連番画像を1つのZIPへまとめて保存します。音声がある入力では、全音声トラックをコピーした音声ファイルも同梱します。MOVに対応する音声のみの場合は `audio.mov`、Opusなどを含む場合は `audio.mka` を使います。ZIP内部の `frame_000001.exr` から始まる番号がフレーム順です。EXR内部のZIP圧縮とは別に、複数ファイルをまとめるZIPアーカイブを作成します。

保存形式を切り替えると、各タブの保存フォルダーとファイル名を保って拡張子が変わります。ProResはMOV、EXRはZIP、HEVCの自動設定はMP4です。画像の保存ダイアログは選択中のTIFF、JPEG XL、AVIFに合わせます。形式の説明は選択欄の直下に表示します。キュー追加時と変換開始時に保存形式を検査します。

動画Logは動画AIと同じ保存設定を使い、表示されているHEVC、ProRes 3種類、PQ EXR、表示基準AP1線形EXRへ書き出します。Log経路の入力解釈は既存のBT.709指定を維持し、欠落していた色変換を適用します。カメラ固有のLogカーブを自動判別する処理ではありません。AP1線形出力はzscaleの基準100 nitに合わせ、EXRのwhiteLuminanceにも100を記録します。ACESの版別出力とHLGは引き続き動画AI用です。

FFmpegのエラー出力は一時ログへ受け取り、フレームの待ち行列は失敗やキャンセルを確認しながら待機します。保存側が失敗した場合は入力側を停止し、FFmpegの具体的なエラーを返します。キューに次の項目があるときは、失敗をログと一覧に残して次へ進みます。

4モードとも今回の作業専用の一時フォルダーへ書き込み、完成後に指定先へ移します。GUIのキャンセルでは未完成出力を破棄し、同名の既存ファイルは保持します。CLIの既存の部分保存設定は動画ファイルに限り維持し、キャンセル時には既存ファイルを置き換えません。EXRは完成したZIPのみ保存します。

静止画のJPEG XLには `distance=0` と16bit RGBを指定し、HDR変換後の画素をロスレス圧縮します。これはSDRからHDRへの変換自体が無変化という意味ではありません。画像Logも動画と同じBT.709入力の解釈で16bit RGBへ変換し、画像AIと共通の保存処理を使います。TIFFの色情報記録に関する既存の制約は残ります。

今回の保存処理に対応するテスト100件が合格しました。元動画（1280×720、241フレーム）もGUIのキューからProRes 422 HQ、4444、4444 XQの3種類へ最後まで保存し、全フレーム数、BT.2020/PQの色情報、入力と一致する読み戻し音声を確認しています。EXRのZIPでは連番と音声の展開確認、JPEG XLでは16bit RGB画素の一致、失敗やキャンセルでは既存ファイルの保持を検証しました。実際のHDR画面での見え方や編集ソフトでの表示評価を実施したという意味ではありません。

EXR系の出力には「保存方法」を追加し、従来のEXR連番ZIPとHDR動画（ProRes 4444 / MOV）を選べるようにしました。初期値はZIPで、動画AIと動画Logの選択は個別に保持します。保存先のフォルダーと名前を保って、拡張子をZIPまたはMOVへ切り替えます。キューへは追加時の保存方法を記録します。CLIでは `--exr-delivery zip` または `--exr-delivery video` を指定します。

HDR動画を選んだ場合は、一時的に作成したEXR連番を読み、表示用のBT.2020/PQへ変換してから既存のProRes 4444保存処理へ渡します。ACEScg 1.3／2.0は、ZIP生成時と同じOpenColorIO内蔵設定と出力変換名を使用し、今回は順方向の公式出力変換を適用します。公式処理のカーブや色域処理は変更しません。表示基準AP1線形は既存の原色変換の逆行列とwhiteLuminanceの基準でBT.2020のnit値へ戻し、PQへ変換します。BT.2020/PQのEXRには追加のACES変換を掛けません。

動画保存はACEScgのデータを保持する方式ではありません。ACEScgを保持したい場合はZIPを選びます。この違いは画面にも表示します。音声がある入力では動画へ音声をコピーし、全工程を終えてから完成したMOVだけを指定先へ移します。途中のEXRや未完成の動画は作業専用の一時フォルダー内に置き、失敗やキャンセルでは既存出力を変更しません。

ZIP／動画選択の追加テスト12件が合格しました。ZIP内の実際のEXR画素を基準に、ACESは対応する公式順方向変換、表示基準AP1は基準輝度を含む変換と比較しています。色変換直後のRGB16とProRes保存後の結果を別々に検査し、圧縮差を色変換の誤りと混同しません。動画のフレーム数、BT.2020/PQの色情報、音声の一致、動画化途中の失敗とキャンセル時の既存ファイル保持を確認しました。GUIから同じACES 2.0設定でZIPとMOVをキューに追加し、両方が完了したことも確認しています。説明文は欄の幅に応じて折り返し、EXR選択時は不要な速度／品質欄を保存方法欄へ切り替えます。

### HDR JPEGとPNGの画像出力

画像AIと画像Logの出力形式にJPEGとPNGを追加しました。JPEGはlibultrahdrの公式エンコーダーで、変換後のBT.2020/PQ画素を10bit RGB入力へ量子化し、SDR画像とHDR復元用ゲインマップを含むUltra HDR JPEGとして保存します。非可逆圧縮のため画素の完全一致は保証しません。拡張子は `.jpg` または `.jpeg` です。`ultrahdr_app` がPATHに必要です（検証版1.4.0、macOSでは `brew install libultrahdr`）。未導入時は変換開始前にエラーを表示します。

PNGは16bit RGBをロスレス保存し、PNG第3版のcICPチャンク（9,16,0,1）でBT.2020、PQ、RGB、フルレンジを明示します。JPEGとPNGのHDR表示には対応する閲覧アプリとHDR表示環境が必要です。Ultra HDR非対応のJPEG閲覧アプリではSDR画像を表示します。

### 入力の複数選択とキュー登録

入力の参照から1件または複数件を選択します。「ファイルを追加」は削除しました。1件の場合は従来どおり出力ファイル名を指定し、複数件の場合は共通の保存先フォルダーを指定します。「キューに追加」で現在の変換設定を各ファイルへ適用し、1ファイルを1行として個別登録します。キューが空の状態で開始した場合も、選択した全件を登録します。複数件の出力名は各入力名を基に生成し、既存ファイルや登録済みの出力名と重なる場合は番号を付けます。形式変更時も保存先フォルダーは維持します。

「キューに追加」ボタンは左下の「キュー開始」のすぐ左に配置しています。右側のキュー上部には削除とクリアの操作を残しています。


### HDR10の明るさ情報を再圧縮せず保存する処理

AI動画の変換後に測定したMaxCLLとMaxFALLは、PyAV 16系のFFmpegバインディングを使ってHEVCの付加情報（SEI）へ記録します。macOSでOpenCVと同じ映像ライブラリを重複ロードしないよう、書き換えは別のPythonプロセスで実行します。従来のlibx265、medium、CRF 0による二度目の圧縮を廃止し、最初に選んだ速度と品質で圧縮した映像データをコピーします。音声データと各パケットの再生時刻も引き継ぎます。既存のマスタリング情報の値は変更しません。

`hevc_metadata.py`が既存の静的HDR情報だけを除去し、新しい値をキーフレームへ追加します。保存先と同じ場所の一時ファイルへ書き終えた後に置き換えるため、途中で失敗した場合は元のファイルを残します。測定値を指定しない既存の色タグ修復処理は従来どおりです。

検証では、CPUの速度と品質の3段階についてMP4とMOVの計6通りを本体から出力し、HDR情報の更新前後で圧縮済みの映像、音声、再生時刻、読み戻した画素の一致を確認しました。CPUとMacのハードウェア出力から作った24フレームのMP4とMOVの計4本でも同じ確認を実施しました。

### 回転情報付き素材とEXR同梱音声の修正

EXRから動画を作る際の幅と高さは、生成済みの最初のEXR画像から取得します。動画Logの読み込み時に90度回転された画像も、保存済み画素と同じ縦横で扱います。フレームレートと音声の入力元は元動画のままです。

EXRのZIPへ同梱する音声は全トラックの方式を確認し、MOVへコピーできる方式のみなら従来のMOV、それ以外はMatroska音声（MKA）を使用します。音声は再圧縮せずコピーします。AACに加えてOpusを含む複数音声トラックでも、先頭トラックだけで保存形式を決めません。

この2件に対する追加検証と既存の入出力テスト計20件が合格しました。回転付き入力ではEXRとMOVの縦横、動画保存へ渡す画素と順序を確認しました。Opus単独、AACとOpus、AACとPCMの3条件では、ZIP保存の完了、全音声トラックの方式、圧縮済み音声データ、読み戻した音声サンプルの一致を確認しました。
